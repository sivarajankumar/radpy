#summary Installing and running RadPy.

=Windows=

==Requirements==

The easiest way to install the [Requirements] on Windows is to install the [http://www.pythonxy.com Python x,y] distribution.  In particular, the following Python x,y modules need to be installed:

* Python
* PyQt
* NumPy
* SciPy
* ETS
* pydicom

==RadPy==
Once the requirements are installed, a source archive can be downloaded from the [http://code.google.com/p/radpy/downloads/list downloads] page, or the latest development version can be checked out from the subversion repository.

===Source archive===
Unzip the source archive to any directory, making sure to maintain the subfolder structure (this should be an option on your unzipping application).  To run RadPy, double click on the run.py file in the RadPy/src/ folder.

===Subversion repository=== 
General instructions for checking out the latest source revision can be found at [http://code.google.com/p/radpy/source/checkout this page].  There are many subversion clients available for Windows.  One good one is [http://tortoisesvn.net/downloads TortoiseSVN].  TortoiseSVN is a Windows shell extension, so 

*